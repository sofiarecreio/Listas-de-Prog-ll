//for (i = 0; texto[i] != '\0'; i++): Inicia um loop for que percorre a string até encontrar o caractere nulo (\0), que indica o fim da string. Enquanto percorre, verifica se o caractere atual é um espaço em branco. Se encontrar, sai do loop com break
//i++: Incrementa o valor de i para avançar para o próximo caractere após o espaço em branco encontrado
//for ( ; texto[i] != '\0'; i++): Inicia outro loop for sem inicialização, que continua a partir do ponto onde o primeiro loop parou. Este loop percorre a string a partir do caractere após o espaço em branco encontrado até o final da string
//printf("%c", texto[i]);: Imprime na tela o caractere atual da string

//Portanto, o que será printado será:

//muito facil